<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.shinseokki.puzzle.dao.ProfileDao">

	<!-- Profile 추가 -->
	<insert id="addProfile" parameterType="com.shinseokki.puzzle.dto.Profile">
		INSERT INTO PROFILE(,
			U_ID, U_PHOTO1, U_PHOTO2, U_PHOTO3, U_PHOTO4, U_PHOTO5) 
			VALUES(#{u_id}, #{u_photo1},#{u_photo2},#{u_photo3, jdbcType=VARCHAR}, 
						#{u_photo4, jdbcType=VARCHAR}, #{u_photo5, jdbcType=VARCHAR})
	</insert>

	<!-- Profile 가져오기 -->
	<select id="getProfile" resultType="com.shinseokki.puzzle.dto.Profile">
		SELECT * FROM PROFILE WHERE K_NAME = #{u_id}
	</select>

	<!-- Profile 수정 -->
	<update id="updateProfile" parameterType="com.shinseokki.puzzle.dto.Profile">
		UPDATE PROFILE SET
			U_PHOTO1 = #{u_photo1}, U_PHOTO2 = #{u_photo2},U_PHOTO3 = #{u_photo3, jdbcType=VARCHAR}, 
			U_PHOTO4 = #{u_photo4, jdbcType=VARCHAR},U_PHOTO5 = #{u_photo5, jdbcType=VARCHAR}
		WHERE U_ID=#{u_id}
	</update>

	<!-- Profile 삭제 -->
	<delete id="deleteProfile" parameterType="com.shinseokki.puzzle.dto.Profile">
		DELETE FROM Keyword
		WHERE K_NAME = #{u_id}
	</delete>

</mapper>
	
	

